{% extends '@App/Layout/layout.html.twig' %}

{% block breadcrumbs %}
    <li class="active">Parse list</li>
{% endblock %}

{% block content %}

    <a href="{{ path('app_view_parselist_parselist_create') }}" class="btn btn-primary">New</a>
    <br>
    <br>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th>name</th>
            <th>city</th>
            <th>link</th>
            <th>sources</th>
        </tr>
        </thead>
        <tbody>
        {% for l in list %}
            <tr>
                <td><a href="{{ path('app_view_parselist_parselist_edit', {'record_id': l.id }) }}">{{ l.name }}</a></td>
                <td>{{ l.city }}</td>
                <td>{{ l.link }}</td>
                <td><a href="{{ path('app_view_parselist_source_list', {'record_id': l.id}) }}">sources</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
