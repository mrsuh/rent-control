{% extends '@App/Layout/layout.html.twig' %}

{% block breadcrumbs %}
    <li>Parse</li>
    <li class="active">Record</li>
{% endblock %}

{% block content %}

    <a href="{{ path('app_view_parse_record_record_create') }}" class="btn btn-primary">New</a>
    <br>
    <br>

    <table class="table table-bordered">
        <thead>
        <tr>
            <th>name</th>
            <th>city</th>
            <th>link</th>
            <th>sources</th>
        </tr>
        </thead>
        <tbody>
        {% for l in list %}
            <tr>
                <td><a href="{{ path('app_view_parse_record_record_edit', {'record_id': l.id }) }}">{{ l.name }}</a></td>
                <td>{{ l.city }}</td>
                <td><a href="{{ l.link }}" target="_blank">{{ l.link }}</a></td>
                <td><a href="{{ path('app_view_parse_record_source_list', {'record_id': l.id}) }}">sources</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
