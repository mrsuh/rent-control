{% extends '@App/Layout/layout.html.twig' %}

{% block content %}

    <table>
        <thead>
        <tr>
            <th>id</th>
            <th>name</th>
            <th>city</th>
            <th>group id</th>
            <th>user id</th>
        </tr>
        </thead>
        <tbody>
        {% for l in list %}
            <tr>
                <td>{{ l.id }}</td>
                <td>{{ l.name }}</td>
                <td>{{ l.city }}</td>
                <td>{{ l.groupId }}</td>
                <td>{{ l.userId }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
